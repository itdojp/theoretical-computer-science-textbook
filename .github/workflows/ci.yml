name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: '3.1'
        bundler-cache: true
        
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: Install dependencies
      run: |
        bundle install
        npm install
        
    - name: Lint Markdown
      run: npm run lint
      continue-on-error: true
      
    - name: Spell check
      run: npm run spellcheck
      
    - name: Check links
      run: npm run check-links
      continue-on-error: true
      
    - name: Build site
      run: npm run build
      env:
        JEKYLL_ENV: production
        
    - name: Test built site
      run: |
        test -f docs/_site/index.html
        test -f docs/_site/src/chapter-1/index.html
        test -f docs/_site/src/appendices/index.html