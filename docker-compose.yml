version: '3.8'

services:
  jekyll:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - .:/app
      - /app/node_modules
      - /app/docs/_site
    environment:
      - JEKYLL_ENV=development
    command: npm run dev